<?php

use Illuminate\Database\Seeder;
use App\User;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {
        // $this->call(UsersTableSeeder::class);

        // DAR UMA LIDA SOBRE ISSO
        // $novoUser = new User();
        // $novoUser->sabe();

        //$users = factory(App\User::class, 3)->make(); // Para testar o banco. Diz quantos usuários vc quer da UserFactory.php
    }
}
